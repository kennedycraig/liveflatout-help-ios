import XCTest

// Placeholder test file — will be replaced in Task 2+.
final class PlaceholderTests: XCTestCase {
    func testPlaceholder() {
        // This test exists solely to allow the test target to compile.
    }
}
